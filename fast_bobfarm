repeat
    wait()
until game:IsLoaded() and game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = workspace.Lobby.Teleport1.CFrame
wait(0.3)
for _ = 1, 750000 do
    game:GetService("ReplicatedStorage").Duplicate:FireServer()
end
wait(1.75)
local v1 = not queueonteleport and (not queue_on_teleport and syn)
if v1 then
    v1 = syn.queue_on_teleport
end
if v1 then
    v1("\r\n                                    loadstring(game:HttpGet(\"https://raw.githubusercontent.com/Skzuppy/forge-hub/refs/heads/main/fast_bobfarm\"))()\r\n                                ")
end
while true do
    local v2, v3, v4 = ipairs(game:GetService("HttpService"):JSONDecode(game:HttpGetAsync("https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Asc&limit=100")).data)
    local v5 = {}
    local v6
    v4, v6 = v2(v3, v4)
    if v4 ~= nil then
        if v6.playing and (type(v6) == "table" and (v6.maxPlayers > v6.playing and v6.id ~= game.JobId)) then
            v5[# v5 + 1] = v6.id
        end
    end
    if # v5 > 0 then
        game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, v5[math.random(1, # v5)])
    end
    wait(2)
end
